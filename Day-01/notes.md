What is DevOps?
-> DevOPs is a way of working where Devlopers(Dev) and Operations(ops) work together to build, test, and deploy software faster, reliable, continuosly and with fewer erros.

.Real life example:
Imagine amazon releases a new feature. with Devops, they can test automatically and deploy it to users in minutes instead of days.

2.Why do companies use DevOps?
->
simple Example:
- without Devops:
. Build software -> test manually -> Deploy manually -> Takes several days.

-with DevOps:
. code -> Automatic testing -> Automatic deployment -> Ready in minutes.

3. What is linux? 
->
linux is operating system just like a windows and macOS , and it is a free and open-source widely used for servers and cloud computing.

4.Why is linux is important?
Most websites and cloud servers run on  linux.
.Example:
.google
.Facebook
.Netflix
.Amazon
.Microsoft azure servers 
.AWS servers

As a devops engineer , you'll use linux to:
.Run servers
.Manage files
.Write automation scripts
.Work with Docker and kubernetes

*Linux is important because most servers in the IT industry ru  on the Linux.

5. What is the terminal?
->
A terminal is a command-line interface used to interact with the operating system using text commands.
or the terminal is a program where you type commands instead of clicking buttons 
instead of using mouse , you can
. create folder
.Delete files
.copy files
.install software 
.Run programs

 Example:
</> Bash 
pwd 
shows you current directory.

*What did you learn today ?
->
i learned :
1. What is devops is
2. Why companies use DevOps
3. What is linux is 
4. Why linux is important
5. what terminal is
6. basic terminal commandss:
- pwd
- ls
- cd
- cd ..
- cd ~
- How to switch drives in git bash using cd /c and cd /d 
7. Gitbash:
Git Bash is a command-line tool for windows that provides a inux-like terminal ans Git commands for software development and version  control.

. Git Bash = Git + Linux terminal on windows.